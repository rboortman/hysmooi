.front.new-front
	.story
		= render 'main_story'
	
	.project_buttons
		= render 'project_buttons' rescue nil

	.offer_images
		= render 'image' rescue nil
	
.lower_buttons
	= render 'new_lower_buttons'